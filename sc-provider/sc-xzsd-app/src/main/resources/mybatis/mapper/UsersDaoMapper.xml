<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.xzsd.app.regist.dao.UserDao">

    <!--统计用户账号数量-->
    <select id="countUserAcct" parameterType="com.xzsd.app.regist.entity.UserInfo" resultType="int">
        select count(user_acct)
        from t_sys_user
        where is_deleted = 0
        and user_acct = #{userAcct}
        <if test="userCode != null and userCode != ''">
            and user_code != #{userCode}
        </if>
    </select>
    <!--校验注册的用户的手机号码是否已存在-->
    <select id="countUserPhone" parameterType="com.xzsd.app.regist.entity.UserInfo" resultType="int">
        select count(phone)
        from t_sys_user
        where is_deleted = 0
        and phone = #{phone}
        <if test="userCode != null and userCode != ''">
            and user_code != #{userCode}
        </if>
    </select>
    <!--校验注册的用户的身份证号码是否已存在-->
    <select id="countIdCard" parameterType="com.xzsd.app.regist.entity.UserInfo" resultType="int">
        select count(idCard)
        from t_sys_user
        where is_deleted = 0
        and id_card = #{idCard}
        <if test="userCode != null and userCode != ''">
            and user_code != #{userCode}
        </if>
    </select>
    <!--校验注册的用户的邮箱是否已存在-->
    <select id="countEmail" parameterType="com.xzsd.app.regist.entity.UserInfo" resultType="int">
        select count(email)
        from t_sys_user
        where is_deleted = 0
        and email = #{email}
        <if test="userCode != null and userCode != ''">
            and user_code != #{userCode}
        </if>
    </select>
    <!--注册用户-->
    <insert id="saveUser" parameterType="com.xzsd.app.regist.entity.UserInfo">
        insert into t_sys_user
        (user_code,
        user_acct,
        user_password,
        user_name,
        sex,
        phone,
        email,
        id_card,
        role,
        is_deleted,
        create_name,
        create_time,
        version
        )
        values
        (#{userCode},
        #{userAcct},
        #{userPassword},
        #{userName},
        #{sex},
        #{phone},
        #{email},
        #{idCard},
        1,
        #{isDeleted},
        #{createName},
        now(),
        0
        )
    </insert>

</mapper>